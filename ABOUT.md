# gym-crm（Resole 顧客管理）

## 一言で言うと

Resole のトレーナーが、お客様1人ずつの体重・目標・トレーニング記録・食事メモ・問診票を管理して、グラフで見られるWebアプリ。参考にした「[デモ用]管理システム」スプレッドシートの見え方・整理の仕方を踏襲し、デザインは resole.jp のトーン（黄色・クリーム・チャコール、ロゴ、Montserrat の英字）に合わせている。お客様向けの画面はない（トレーナー専用）。

分析資料・要件定義は `~/⭐Claude専用/032_gym-crm/docs/` にある（GitHub には入れていない）。

GitHub：https://github.com/kagami-fit/gym-crm
公開デモ：https://resole-gym-crm.netlify.app （リンクを知っている人が「デモを見る」からログインなしで見られる。架空のデータのみ）

## 何ができるのか

- **顧客一覧（台帳）**：検索、ステータス（在籍中／体験／休会中／退会）で絞り込み、最終来店日・現体重・目標までの差
- **顧客ページ（1人1ページ）**：上部の「基準日（来店日）」を起点に、すべてのタブが「直近1週間・1ヶ月」などを表示
  - 概要：基本データ（体組成・基礎代謝・目標カロリー・PFC・水分・食材の目安）、目標の進捗（予定ペースとの差）、最近の会話メモ（6件）、体重・体脂肪率の推移、減量予測（月単位・週単位）、トレーニングの総負荷量、直近7日の食事メモ
  - 体重・目標：7日分の体重・体脂肪率をまとめて入力、目標の設定（変更は履歴として残る）
  - トレーニング：部位・種目・重さ・回数・セット数を記録（前回の内容を読み込める、種目ごとに前回値を表示、重さ・回数・セットは −／＋ ボタンでも入力できる）、総負荷量・推定1RM を自動計算、部位別・種目別のグラフ
    - 画面の上に**目的・目標・注意事項を固定表示**（目的＝台帳の目的と問診票の「なりたい姿」「期限」、目標＝目標体重・あと何kg・予定ペースとの差、注意＝問診で「あり」の項目）。「詳しく」で目標のメモや問診の詳細まで広げる。帯の右に「前回の手書き」「今日の手書き」「会話メモ」のボタン
    - 記録の一覧の各回の下に**手書きメモを半分ほど表示**（書いてある所から、高さは最大でページの半分。短いメモは書いてある所だけ）。タップするとすぐ全体を大きく表示し、「前の回」「次の回」で切り替え、「書き足す」で書く画面へ（画面に近づいたメモから読み込み、直近2回分は先に読み込んでおく）
  - **手書きメモ（セッションごと）**：Apple Pencil・指で書ける全画面のノート（黒・赤・青・マーカー、太さ3段階、消しゴム、元に戻す／やり直す、ページ追加）。書いて2秒後に自動保存。Apple Pencil を使うと自動で「ペンだけで書く」になり、手のひらが触れても線が引かれない。横向きは横幅いっぱいに大きく表示して指でスクロール。前回の手書きメモを横に出して見比べられる。見出しの下に目的・目標・注意事項、道具の並びに「会話メモ」ボタン
  - **会話メモ**：会話で出た内容を日付と一緒に残す。種類は「会話」「変化（通って変化を感じたこと）」「良かったこと」「ネガティブ（言動・不満・不安）」「身体の変化」の5つ。種類で絞り込み、キーワードで探せる。専用のタブのほか、トレーニングの画面・手書きメモの画面の「会話メモ」ボタンから、その場で書いて見返せる
  - 食事メモ：7日×朝・昼・夕・間食・メモ
  - 台帳：基本情報・連絡先・緊急連絡先・目的など
  - 問診票：健康状態・既往歴・痛み・運動歴・生活習慣・食習慣（「あり」の項目は顧客ページ上部に注意事項として表示）
- **体験シミュレーター**：入会前のお客様に、その場で目標カロリーと達成見込みを見せる（保存しない）。そのまま顧客登録へ引き継げる
- **設定**（変更はオーナーのみ）
  - 計算式：基礎代謝の式（除脂肪体重×係数／Katch-McArdle／国立健康・栄養研究所／ハリス・ベネディクト）と係数、たんぱく質・脂質・水分・食材の目安、体脂肪1kgのカロリー、推定1RMの式
  - 減量ペースと活動係数の選択肢（追加・変更できる）。変更は全顧客・全画面の計算に同じように反映される
  - 種目マスタ（6部位132種目。追加・並べ替え・使用停止）
  - スタッフのアカウント作成・権限・パスワード変更

- **iPad 向けの作り**（主に iPad で使う前提）
  - ボタン・入力欄は指で押しやすい高さ（44px）。iPad は縦・横とも上部のメニュー、顧客ページのタブは画面上部に固定
  - iPad 縦向きでは顧客一覧をカード表示、食事メモを1日ごとのカードで入力
  - Safari の「共有 → ホーム画面に追加」で、Resole のアイコンからアプリのように全画面で開ける

## 構成

| パス | 役割 |
|---|---|
| `app/login` | ログイン画面 |
| `app/(app)/clients` | 顧客一覧・新規登録・顧客ページ（`[id]` 配下に各タブ） |
| `app/(app)/simulator` | 体験シミュレーター |
| `app/(app)/settings` | 設定（計算式・減量ペース／種目マスタ／スタッフ） |
| `lib/calc/` | **計算の中心**。`settings.ts`（設定の項目と初期値）、`body.ts`（体組成・目標カロリー・予測・進捗）、`training.ts`（総負荷量・1RM・集計） |
| `lib/data/` | データベースの読み書き（`overview.ts` が顧客ページの計算をまとめる） |
| `lib/questionnaire.ts` | 問診票の項目定義（ここを書き換えると画面と保存内容が変わる） |
| `components/charts/` | グラフ（体重推移・予測・総負荷量・部位別/種目別・PFC） |
| `components/client/` | 顧客ページの部品 |
| `components/drawing/` ・ `lib/drawing.ts` | 手書きメモ（書く画面 `DrawingPad`、表示 `DrawingView`、一覧の半分表示 `MemoPreview`＋タップで大きく表示（ともに `MemoViewer.tsx`）、間引いた線の表示 `DrawingThumb`、保存形式と筆圧つきの線の描画、一覧用に線を間引く `drawingThumb`・見せる範囲 `previewCrop`） |
| `app/api/memos/[sessionId]` | 手書きメモ全体を返す（一覧のメモが画面に近づいたとき・タップしたときに読み込む） |
| `components/client/FocusBar.tsx` ・ `lib/data/focus.ts` | トレーニングの画面上部に固定する「目的・目標・注意事項」の帯 |
| `components/client/TalkNotes.tsx` ・ `lib/data/talk.ts` ・ `app/(app)/clients/[id]/talk` | 会話メモ（一覧・追加・変更・削除・絞り込み・検索。種類は `lib/labels.ts` の `TALK_KINDS`） |
| `components/Sheet.tsx` | 右から出るパネル・全画面の表示（会話メモ・手書きメモの表示で共通） |
| `app/(focus)/memo/[sessionId]` | 手書きメモの全画面（メニューなし） |
| `prisma/schema.prisma` | データベース設計／`prisma/seed.ts` で初期設定・種目マスタ・デモ顧客（会話メモ・手書きメモの見本つき）を投入 |
| `prisma/seed-data/demo-memos.json` ・ `scripts/gen-demo-memos.py` | デモ用の手書きメモの見本（8件）と、その作り方（手書き風フォント Klee One の文字をペンの線に変換。作り直すときだけ使う） |
| `scripts/dev.mjs` | `npm run dev`：開発用DB（`.data/postgres`）と開発サーバーを一緒に起動（`scripts/setup.mjs` は初回セットアップ） |
| `lib/demo.ts` ・ `app/api/demo/login` | 公開デモの「デモを見る」（Netlify の環境変数 `DEMO_MODE=true` のときだけ有効） |
| `netlify.toml` ・ `scripts/deploy-netlify.sh` | Netlify への公開（ビルド中は手元の `.env` などを退避して、公開物に秘密の値を入れない） |
| `.env.production.local` | 公開デモのデータベース（Neon）の接続先。手元だけ・共有しない（`prod:*` のコマンドが読む） |
| `scripts/lan-proxy.mjs` ・ `lib/dev-hosts.mjs` ・ `lib/dev-access.ts` | 開発中に同じWi-Fiの iPad から開くための中継と、開発用ログインを「この Mac だけ」に限る仕組み |
| `scripts/create-user.ts` | ログイン用アカウントの作成（画面の「設定 → スタッフ」からも作れる） |
| `tests/` | テスト（計算が参考シートと同じ数値になること、手書きデータの検査、一覧用の間引きと見せる範囲、会話メモの種類など、28件） |
| `README.md` | GitHub のトップに出る紹介と動かし方（初めて見る人向け） |

技術：Next.js 16 / TypeScript / Prisma 6 / PostgreSQL（開発中はプロジェクト内に保存する開発用DB、本番は Neon 想定）/ Better Auth / Tailwind CSS 4 / Recharts。構成は `~/Dev/kuchikomi-tool` と同じ。

## 使い方

```bash
# 初回だけ
nvm use 22
npm install
cp .env.example .env   # 新しい Mac で始めるとき（BETTER_AUTH_SECRET と DEV_OWNER_PASSWORD を書き換える）
npm run setup          # 開発用DBの作成・テーブル作成・初期データ（設定・種目・開発用オーナー・デモ顧客4人）

# 毎回
npm run dev            # 開発用DB＋開発サーバー → http://localhost:3020
```

- 開発中は、ログイン画面の「開発用オーナーでログイン」で入れる（`.env` の `DEV_LOGIN=true` のときだけ。この Mac で開いたときだけ出る。本番では出ない）
- チェック：`npm run typecheck` / `npm run lint` / `npm test` / `npm run build`
- デモ顧客（デモ 太郎・花子・次郎・美咲）は `SEED_DEMO=1` かつ顧客が0人のときだけ入る。本番運用前に削除する

### iPad で開く（開発中・同じWi-Fi）

1. Mac で `npm run dev` を起動すると、「iPad で開く（同じWi-Fi）: http://192.168.x.x:3020」と表示される（今の Mac なら `http://192.168.1.221:3020`。Wi-Fi が変わるとアドレスも変わる）
2. iPad を同じWi-Fiにつないで、Safari でそのアドレスを開く
3. ログインはメールアドレスとパスワードで（「開発用オーナーでログイン」は安全のため Mac でしか出ない）。アカウントは Mac 側で「設定 → スタッフ → アカウントを作成」で作る
4. Safari の「共有 → ホーム画面に追加」でアイコンから開ける（ホーム画面から開いたときは、初回だけもう一度ログインが必要）

- 開発サーバーの本体は Mac の中だけで待ち受け、iPad からの通信は中継（`scripts/lan-proxy.mjs`）を通る。中継を通った通信では開発用ログインが使えない
- Wi-Fi に公開したくないときは `LAN=0 npm run dev`
- 開発サーバーは試用のためのもの。お客様の本当のデータは本番公開後に入れる

### 環境変数（`.env`、見本は `.env.example`）

| 名前 | 内容 |
|---|---|
| `DATABASE_URL` | PostgreSQL（本番は Neon など） |
| `BETTER_AUTH_SECRET` / `BETTER_AUTH_URL` | ログイン用の秘密鍵と公開URL |
| `DEV_LOGIN` / `DEV_OWNER_EMAIL` / `DEV_OWNER_PASSWORD` | 開発用オーナー（本番では設定しない） |
| `SEED_DEMO` | `1` でデモ顧客を投入 |

### 公開デモ（リンクを知っている人が見られる）

- URL：**https://resole-gym-crm.netlify.app**。ログイン画面の「デモを見る（ログイン不要）」から入れる。GitHub のページの「Website」のリンクからも開ける
- 中身は架空のデモ顧客だけ。「デモを見る」で入るのはスタッフ権限のデモ用アカウントなので、設定の変更・アカウント作成・顧客の削除はできない。画面の上に「デモ版です」と出る
- **本当のお客様の情報は入れない**（リンクを知っていれば誰でも入れるため）
- 公開先：Netlify 無料プラン（プロジェクト `resole-gym-crm`、チーム kagami-fit）。**クチコミツールと同じチームなので、無料枠（月300クレジット）を共有**する。使い切ると両方とも月末まで止まる
- データベース：Neon 無料プラン（プロジェクト `resole-gym-crm`＝calm-dust-36433090、米国オハイオ、PostgreSQL 17）
- Netlify の環境変数：DATABASE_URL（プール経由）、DATABASE_URL_UNPOOLED、BETTER_AUTH_SECRET、BETTER_AUTH_URL、BETTER_AUTH_TRUSTED_ORIGINS、DEMO_MODE、DEMO_USER_EMAIL、DEMO_USER_PASSWORD（DEV_* は入れない）

```bash
npm run deploy:preview   # 確認用のURLに公開（クレジットを使わない）
npm run deploy:prod      # https://resole-gym-crm.netlify.app に公開（1回15クレジット）
npm run prod:migrate     # 公開デモのDBのテーブルを変えたとき
npm run prod:seed        # 公開デモのDBに初期データ（設定・種目・デモ用アカウント・デモ顧客。顧客が0人のときだけデモ顧客を入れる）
```

### 本番運用に切り替えるとき（本当のお客様の情報を入れる前に）

1. データの保存場所を決める（今の公開デモのDBは米国。健康情報を扱うので、国内リージョンなども検討する）
2. Netlify の環境変数から `DEMO_MODE`・`DEMO_USER_*` を消し、デモのデータを消したDBにする（`SEED_DEMO` なしで `npm run prod:seed`）
3. `npm run prod:user -- --email ... --name ... --role owner` でオーナーを作成し、スタッフは「設定 → スタッフ」で作る
4. `npm run deploy:prod`（健康情報を扱うため、ログイン必須のまま運用する）

## 状態

| 対象 | 状態 |
|---|---|
| 顧客台帳・問診票・体重/目標・トレーニング記録・食事メモ・グラフ・設定・体験シミュレーター | 動作確認済み（2026-09-25、開発環境） |
| iPad 対応・セッションごとの手書きメモ | 動作確認済み（2026-09-25、iPad 縦 820×1180／横 1180×820 の画面サイズで確認。実機の Apple Pencil での確認はこれから） |
| iPad 実機から開く（同じWi-Fi） | 準備済み（2026-09-25、Mac の Wi-Fi アドレス経由でログイン画面・画面の自動更新・ログインの受け付けを確認） |
| デザイン | resole.jp のトーン＆マナーに合わせて作成済み |
| 初回カウンセリングのカルテ | 未着手（内容の共有待ち。`lib/questionnaire.ts` と同じ方式で追加予定） |
| SOAP・姿勢評価・AIフィードバック・契約/回数券/予約 | 今回は入れない（要件定義で除外） |
| 公開デモ（Netlify・Neon） | 公開済み（2026-09-25）https://resole-gym-crm.netlify.app 。架空のデータのみ、「デモを見る」でログインなしに入れる |
| 目的・目標の固定表示／一覧の手書きメモ（半分表示）／会話メモ | 動作確認済み・公開デモに反映済み（2026-09-26。公開DBにテーブル追加とデモの会話メモを投入） |
| 本番運用（本当のお客様の情報） | 未着手（データの保存場所・アカウントを決めてから。手順は上の「本番運用に切り替えるとき」） |
